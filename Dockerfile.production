FROM node:24-alpine

WORKDIR /app

COPY package*.json ./

RUN npm ci

COPY . .

RUN npm run build

# Fastify serves the built SPA (dist/), the bundled product images and /api.
CMD ["npm", "run", "start:prod"]
